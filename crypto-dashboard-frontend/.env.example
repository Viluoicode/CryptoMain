# C# Backend API base URL (used by the frontend API client).
# Change this if your backend runs on a different host / port.
# Defaults to https://localhost:44386 when not set.
VITE_API_BASE_URL=https://localhost:44386
